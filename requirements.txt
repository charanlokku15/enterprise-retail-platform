# For scripts you run OUTSIDE the Airflow containers, e.g. local
# data-generation, ad-hoc DuckDB queries, or Spark job development.
# Airflow's own dependencies are installed inside its container via
# _PIP_ADDITIONAL_REQUIREMENTS in .env — keep both lists in sync.

duckdb==1.0.0
dbt-duckdb==1.8.1
great-expectations==0.18.19
minio==7.2.7
faker==25.8.0
psycopg2-binary==2.9.9
pandas==2.2.2
pyarrow==16.1.0
pyspark==3.5.1
fastapi==0.111.0
uvicorn==0.30.1
